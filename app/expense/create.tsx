import { PlaceholderScreen } from '../../components/PlaceholderScreen';

export default function CreateExpenseScreen() {
  return (
    <PlaceholderScreen
      title="Создать расход"
      description="Форма создания расхода будет реализована на следующем этапе."
    />
  );
}
